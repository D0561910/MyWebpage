<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Loding Animation</title>
    <style>
        body {
            background: #fadee1;
        }

        .loader {
            width: 300px;
            height: 300px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: transparent;
        }

        .line-area {
            position: absolute;
            width: 80px;
            height: 80px;
            left: calc(50% - 80px/2);
            top: calc(50% - 80px/2);
            font-size: 0;
            text-align: center;
        }

        .line-area div {
            width: 6px;
            height: 80px;
            background: #262626;
            display: inline-block;
            margin: 0 4px;
            animation: animate 1s linear infinite;
        }

        .line-area div:nth-child(2) {
            animation-delay: -.8s;
        }

        .line-area div:nth-child(3) {
            animation-delay: -.7s;
        }

        .line-area div:nth-child(4) {
            animation-delay: -.6s;
        }

        .line-area div:last-child {
            animation-delay: -.5s;
        }

        @keyframes animate {

            0%,
            10%,
            80%,
            100% {
                transform: scaleY(.3);
            }

            30%,
            60% {
                transform: scaleY(1);
            }
        }
    </style>
</head>

<body>
    <div class="loader loading-area">
        <div class="line-area">
            <div></div>
            <div></div>
            <div></div>
            <div></div>
            <div></div>
        </div>
    </div>
</body>

</html>